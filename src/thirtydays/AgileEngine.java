package thirtydays;

/***
public class AgileEngine {

    Map<Long, Long> convertToResultMap(Map<String,Integer> store){
        Map<Long, Long> result= new HashMap<>();
        return result;
    }
    Map<Long, Long> count(Map<String, Integer>...visits){
        Map<Long, Long> result= new HashMap<>();
        for (int i = 0; i < visits.length; i++) {
             //visits[i].entrySet().forEach(
                  //(stringIntegerEntry -> {if (stringIntegerEntry.getKey() == null) }));}
         return result;
    } **/

//}
